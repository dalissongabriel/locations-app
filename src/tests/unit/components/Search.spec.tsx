import { describe, expect, it } from "vitest";

describe("Search component", () => {
  it("should be equal numbers", () => {
    const result = 1;
    const expected = 1;

    expect(result).to.be.deep.equal(expected);
  });
});
